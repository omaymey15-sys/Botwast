package com.neogame.psp.emulator

/**
 * Contrôleur IA
 * Gère l'IA des ennemis
 */
class AIController {
    fun update(enemies: List<GameEngine.GameObject>) {
        enemies.forEach { enemy ->
            // IA simple - patrouille
        }
    }
}